<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aditya.prototypehabba">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBceBq_-ZO7qB_z0oTVmt3xB7uSjBZS5pA " />

        <activity android:name=".map.Intro_Slider.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".map.ResideMenu.MainActivity" />
        <activity android:name=".map.Registration.Registration" />
        <activity android:name=".map.SlideMenu.SlideMenu" />
        <activity android:name=".map.Scroll.Scroll" />
        <activity android:name=".map.GridviewMenu.card1.Dance" />
        <activity android:name=".map.GridviewMenu.card2.Design_and_Digital_Arts" />
        <activity android:name=".map.GridviewMenu.card3.Dramatics" />
        <activity android:name=".map.GridviewMenu.card4.Gaming" />
        <activity android:name=".map.GridviewMenu.card5.Intracollege" />
        <activity android:name=".map.GridviewMenu.card6.Lifestyle" />
        <activity android:name=".map.GridviewMenu.card8.Management" />
        <activity android:name=".map.GridviewMenu.card7.Literary" />
        <activity android:name=".map.GridviewMenu.card9.Miscellaneous" />
        <activity android:name=".map.GridviewMenu.card10.Music" />
        <activity android:name=".map.GridviewMenu.card11.Technical" />
        <activity android:name=".map.Map.MapsActivity" />
        <activity android:name=".map.Notification.NotificationsFragment" />
        <activity
            android:name=".map.Calender.Calendar"
            android:label="@string/title_activity_calendar"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>